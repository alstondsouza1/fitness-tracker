<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Details</title>
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
</head>
<body>
    <header>
        <nav>
            <a href="/workouts">Home</a>
        </nav>
    </header>

    <main>
        <h1><%= workout.type %> on <%= new Date(workout.date).toLocaleString() %></h1>
        <p>Duration: <%= workout.duration %> minutes</p>
        <p>Calories Burned: <%= workout.calories_burned || 'N/A' %></p>
        <p>Notes: <%= workout.note || 'No additional notes' %></p>

        <form action="/workouts/<%= workout.id %>?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
        </form>
        <a href="/workouts">Back to Log</a>
    </main>

    <footer>
        &copy; <%= new Date().getFullYear() %> Fitness Tracker. All rights reserved.
    </footer>

    <script src="/scripts/dark-mode.js"></script>
</body>
</html>
